<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>TextAnalyzer</title>
        <link rel="stylesheet" href="~/StyleSheet.css">
    </head>
    <body>
        @model MvcMovie.Views.HelloWorld.IndexModel
        @{
            ViewData["Title"] = "Index";
        }

        <h2>TextAnalyser</h2>

        <p>Type your text right here!</p>
        <form asp-action="Index" method="post" enctype="multipart/form-data">

            <div class="form-group">
                <label class="control-label">Text</label>
                <input id="answerid" name="answerid" class="form-control" />
                <span class="text-danger"></span>
            </div>

            <input type="submit" value="Analyze" class="btn btn-default" />

        </form>
        @if (Model.Answer != null) { <p>Your keywords: @Model.Answer</p> }
        @*<p>@Model.Answer.Split()[2]</p>
        <p>@Model.Answer.Split()[3]</p>
        <p>@Model.Answer.Split()[4]</p>
        <p>@Model.Answer.Split()[5]</p>
        <p>@Model.Answer.Split()[6]</p>*@
        @*<form asp-action="Post" method="post" enctype="multipart/form-data">

                     <div class="form-group">
                         <label class="control-label">Your speed</label>
                         <input class="form-control" />
                         <span class="text-danger"></span>
                     </div>

                     <input type="submit" value="Analyze" class="btn btn-default" />

                 </form>
        <p>Время, потраченное на прочтение текста: @Html.DisplayFor(model => model.Answer2)</p>*@


        @*@Html.DisplayFor(model => model.Answer)*@
    </body>
</html>